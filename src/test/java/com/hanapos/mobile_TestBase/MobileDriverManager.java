package com.hanapos.mobile_TestBase;

public class MobileDriverManager {
  /*  private static final ThreadLocal<AppiumDriver<MobileElement>> mobileDriver = new ThreadLocal<>();

    public static void setDriver(AppiumDriver<MobileElement> driver) {
        mobileDriver.set(driver);
    }

    public static AppiumDriver<MobileElement> getDriver() {
        return mobileDriver.get();
    }

    public static void quitDriver() {
        if (mobileDriver.get() != null) {
            mobileDriver.get().quit();
            mobileDriver.remove();
        }
    }*/
}
